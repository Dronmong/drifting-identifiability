# Calibrated conservative bridge results

**Status:** implementation and registered B1 experiment pending.

Authoritative artifacts will be stored under `numerics/bridge_runs/`. The
result will be evaluated as a conjunction of the frozen gates in
`CalibratedBridgeProtocol.md`; an isolated favorable arm or checkpoint cannot
be promoted.
