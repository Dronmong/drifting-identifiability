# Frozen CAP2/ASFD production and evaluation environment.
# Use CPython 3.11.13 (the newest 3.11 the pinned uv 0.8.14 can provide). The CUDA build is explicit so a provider cannot silently
# install CPU-only torch from PyPI.
--extra-index-url https://download.pytorch.org/whl/cu126
torch==2.7.1+cu126
torchvision==0.22.1+cu126
numpy==1.26.4
pillow==12.2.0
clean-fid==0.1.35
setuptools==83.0.0
ninja==1.13.0
pyspng==0.1.4
# Imported at the top of the vendored StyleGAN2-ADA legacy.py, which the
# positive control loads through legacy.load_network_pkl. A CLI argument
# parser with no role in training, metrics or the model; pinned here because
# the positive control cannot import without it.
click==8.4.2
